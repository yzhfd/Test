<h1>Keyword creation</h1>

<form action="{{ path('keyword_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('keyword') }}">
            Back to the list
        </a>
    </li>
</ul>
