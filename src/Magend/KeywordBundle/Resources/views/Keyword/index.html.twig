<h1>Keyword list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Keyword</th>
            <th>Createdat</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('keyword_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.keyword }}</td>
            <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif%}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('keyword_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('keyword_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('keyword_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
