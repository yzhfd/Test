{% extends app.request.isXmlHTTPRequest() ? '::ajaxBase.html.twig' : '::base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/test.js') }}" type="text/javascript"></script>
{% endblock %}

{% block main %}
    <a rel="furnitureDetailHots" class="hot_add" href="#">+单品家具展示热区</a>
    <a rel="slideLayerHots" class="hot_add" href="#">+滑动图层热区</a>
    <a rel="furnitureSkinnable" class="hot_add" href="#">+家具换肤热区</a>
    <form style="width:680px; margin-top:10px;" action="{{ path('page_test') }}" method="post" {{ form_enctype(form) }}>
        {#
        <div class="clearfix">
            {{ form_label(form.name) }}
            <div class="input">{{ form_errors(form.name) }}{{ form_widget(form.name, { 'attr':{'class':'middle'} }) }}</div>
        </div>
        
        <div class="clearfix">
            {{ form_label(form.landscapeCoverImage) }}
            <div class="input">{{ form_errors(form.landscapeCoverImage) }}{{ form_widget(form.landscapeCoverImage) }}</div>
        </div>
        
        <div class="clearfix">
            {{ form_label(form.portraitCoverImage) }}
            <div class="input">{{ form_errors(form.portraitCoverImage) }}{{ form_widget(form.portraitCoverImage) }}</div>
        </div>
        
        <div class="clearfix">
            {{ form_label(form.stdProperty.name) }}
            <div class="input">{{ form_widget(form.stdProperty.name) }}</div>
        </div>
        
        {{ form_row(form.hotContainer.furnitureDetailHots.vars.prototype) }}
        
        #}
        
        
        {{ form_widget(form) }}
        
        <div class="actions"><button class="btn primary" type="submit">提交</button></div>
    </form>
    
{% endblock %}